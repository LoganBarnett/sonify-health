// Re-export from foundation.  Kept for backward compatibility of
// existing `use sonify_health_lib::logging::*` imports inside the
// cli, server, and lib crates.
pub use rust_template_foundation::logging::*;
