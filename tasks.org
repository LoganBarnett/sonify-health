#+title:     sonify-health Tasks
#+language:  en

* DONE Sync with rust-template (420397e..c3d6e14+)
Forward-port changes from rust-template that landed after the last synced
commit (=5f606c5=).  The following commits need review:

- =420397e= docs: add task for pre-commit hook treefmt scoping bug
  - =tasks.org= only — no code to forward-port, but check if the treefmt
    scoping bug applies to sonify-health's pre-commit hook too.
- =2d71318= feat: use native CA store for TLS instead of webpki-roots
  - Already applied directly to sonify-health (=5330281=).  No action needed.
- =e681440= fix: use Debug format for OIDC discovery error to preserve cause chain
  - Already applied directly to sonify-health (=cea961d=).  No action needed.
- =66d819e= feat: add justfile with build, test, and run recipes
  - Add =pkgs.just= to =flake.nix= devShell.
  - Create a =justfile= adapted for sonify-health (crate names, Elm paths).
  - The Elm import fix (=Url.Parser.map=/=s=) is template-specific and does not
    apply here.
- =c3d6e14= fix: pass all args through in justfile run recipe
  - Include when creating the justfile above.

Also include any commits that land after =c3d6e14= before this task is started.

After syncing, update =rust-template.json= with the final synced hash.

* TODO Pre-commit hook formats entire tree instead of staged files only
The =.cargo-husky/hooks/pre-commit= hook passes staged filenames as
positional arguments to =treefmt=, but treefmt may ignore positional arguments
and use its own tree walker.  This means =treefmt $STAGED_FILES= could format
every file in the repository, not just the staged ones.  Unstaged files get
reformatted on disk but are not re-staged, leaving dirty working tree
artifacts that have to be committed separately.

Forward-ported from rust-template (=420397e=).  The current treefmt version
shows =<paths>...= as a positional argument in =treefmt --help=, so the bug
may already be resolved.  Verify before implementing a fix.

Fix (if needed): use =treefmt --paths= (or the appropriate flag for limiting
treefmt to specific files) so only staged files are reformatted.  If treefmt
does not support per-file invocation, consider an alternative approach such as
running the language-specific formatters directly on the staged files.

* TODO Filter slope (pole count)
Discrete choice (2/4 poles).  4-pole = 24 dB/oct, much steeper cutoff.
Chain two =lowpass()= nodes with same cutoff/Q.  Could be a voice param with
discrete steps or a boolean "steep filter".

* TODO Square/pulse oscillator
Add square wave option for hollow, odd-harmonic tone (warp core character).
Either a fourth oscillator weight (=square_ratio=) or replace saw with a
pulse-width param.

* TODO Vibrato depth + rate
LFO pitch modulation after chirp onset.  Two params: =vibrato_depth= (cents)
and =vibrato_rate= (Hz).  Adds "living machine" movement to long sustains.

* TODO Voice-level detune
User-facing detune offset (cents) applied to all boops, independent of
severity.  Creates chorus-like thickness on healthy tones.

* TODO Voice-level harshness offset
User-facing offset added to severity-driven harshness.  Lets user push healthy
tones buzzier or pull down tones smoother.
